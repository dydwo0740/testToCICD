version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/cicdproject
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
#  ApplicationStop:
#  BeforeInstall:
#  AfterInstall:
  ApplicationStart:
    - location: deploy.sh
      timeout: 60
      runas: ubuntu
#  ValidateService:
