version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/cicdproject
    overwrite: yes

permissions:
  - object: /home/ec2-user/cicdproject
    owner: ubuntu
    group: ubuntu

hooks:
#  ApplicationStop:
#  BeforeInstall:
#  AfterInstall:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 60
      runas: ubuntu
#  ValidateService:
